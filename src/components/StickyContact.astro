---
import { CONTACT_INFO, getWhatsAppLink } from '../data/config'
---

<div class="fixed inset-x-0 bottom-0 z-50 px-4 pb-4 md:inset-x-auto md:bottom-6 md:right-6 md:px-0 md:pb-0">
  <div class="mx-auto grid w-full max-w-lg gap-2 rounded-2xl bg-white/95 p-2 shadow-2xl ring-1 ring-slate-200 md:w-72 md:rounded-3xl md:p-3">
    <a
      href={getWhatsAppLink()}
      target="_blank"
      class="flex min-h-[48px] items-center justify-center rounded-xl bg-emerald-500 px-4 text-sm font-extrabold text-white shadow-lg shadow-emerald-500/30 md:rounded-2xl"
    >
      WhatsApp comercial
    </a>
    <a
      href={`tel:${CONTACT_INFO.phone}`}
      class="flex min-h-[48px] items-center justify-center rounded-xl border border-slate-200 bg-white px-4 text-sm font-extrabold text-slate-900 md:rounded-2xl"
    >
      Llamar ejecutiva
    </a>
  </div>
</div>
