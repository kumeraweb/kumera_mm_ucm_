---
import { getWhatsAppLink } from "../data/config";
import { PLANES } from "../data/planes";
---

<section
  id="planes"
  class="bg-gradient-to-b from-white via-blue-50/70 to-white py-12 md:py-16"
>
  <div class="mx-auto w-full max-w-6xl px-4">
    <div class="max-w-2xl">
      <p
        class="text-[11px] font-bold uppercase tracking-[0.2em] text-slate-500"
      >
        Planes de contratación
      </p>
      <h2 class="mt-3 text-2xl font-extrabold text-slate-900 md:text-3xl">
        Elige tu plan de rescate
      </h2>
      <p class="mt-3 text-base text-slate-600">
        Contratación comercial de planes de rescate en ambulancias privadas y cobertura extrahospitalaria.
        No es un servicio de emergencia ni atención médica integral.
      </p>
    </div>

    <div class="mt-12 grid gap-8 md:grid-cols-2 lg:gap-12">
      <!-- Copy fijo de planes por requerimiento de compliance -->
      {
        PLANES.map((plan) => (
          <article
            class={`relative flex flex-col justify-between overflow-hidden rounded-[2rem] border ${plan.destacado ? "border-red-200 bg-white ring-8 ring-red-50/60" : "border-slate-200 bg-white"} p-8 transition-all hover:shadow-xl hover:shadow-blue-900/5`}
          >
            {plan.destacado && (
              <div class="absolute right-0 top-0 rounded-bl-2xl bg-red-600 px-4 py-2 text-[10px] font-bold uppercase tracking-[0.2em] text-white">
                {plan.etiqueta || "Recomendado"}
              </div>
            )}

            <div>
              <div class="mb-6">
                <span class="text-xs font-bold uppercase tracking-wider text-slate-400">
                  {plan.id === "plan-senior" ? "Adultos 75+" : "Todo público"}
                </span>
                <h3 class="mt-2 text-3xl font-extrabold text-slate-900">
                  {plan.nombre}
                </h3>
                <p class="mt-3 text-sm leading-relaxed text-slate-600">
                  {plan.descripcion}
                </p>
              </div>

              <div class="mb-8 flex items-baseline gap-3 border-b border-slate-100 pb-8">
                <span class="text-5xl font-extrabold tracking-tight text-slate-900">
                  {plan.precio}
                </span>
                <span class="text-xl font-extrabold text-blue-700">UF</span>
              </div>

              <ul class="mb-8 space-y-4">
                {plan.features.map((feat) => (
                  <li class="flex items-start gap-3 text-sm text-slate-700">
                    <svg
                      class="mt-0.5 h-5 w-5 flex-shrink-0 text-red-500"
                      fill="none"
                      viewBox="0 0 24 24"
                      stroke="currentColor"
                      stroke-width="2.5"
                    >
                      <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        d="M5 13l4 4L19 7"
                      />
                    </svg>
                    <span>{feat}</span>
                  </li>
                ))}
              </ul>
            </div>

            <div>
              <a
                class={`group flex w-full items-center justify-center rounded-xl py-4 text-sm font-bold transition-all ${plan.destacado ? "bg-blue-700 text-white hover:bg-blue-800 shadow-lg shadow-blue-600/20" : "bg-slate-100 text-slate-900 hover:bg-slate-200"}`}
                href={getWhatsAppLink(
                  `Hola, quiero contratar el ${plan.nombre}.`,
                )}
                target="_blank"
              >
                CONTRATAR ONLINE
              </a>
              <p class="mt-4 text-center text-[10px] text-slate-400">
                Sujeto a factibilidad técnica y condiciones del proveedor.
                <>
                  <br />
                  <span class="font-bold text-red-500">
                    NO válido para emergencias sin contrato activo.
                  </span>
                </>
              </p>
            </div>
          </article>
        ))
      }
    </div>

    <div class="mt-12 grid gap-8 lg:grid-cols-[1.1fr_0.9fr] lg:items-center">
      <div>
        <p class="text-xs font-bold uppercase tracking-[0.2em] text-slate-400">
          Transparencia comercial
        </p>
        <h3 class="mt-3 text-2xl font-extrabold text-slate-900">
          Contratación clara y sin urgencias
        </h3>
        <p class="mt-4 text-sm text-slate-600 leading-relaxed">
          Este sitio es un canal comercial independiente para la contratación de planes de rescate.
          Los servicios operativos son prestados por proveedores externos bajo sus propios términos
          y tiempos de respuesta. No atendemos emergencias inmediatas.
        </p>
        <p class="mt-4 text-xs font-semibold text-red-600">
          NO válido para emergencias sin contrato activo.
        </p>
        <a
          class="mt-6 inline-flex items-center justify-center rounded-full bg-red-600 px-6 py-3 text-xs font-extrabold uppercase tracking-widest text-white shadow-lg shadow-red-600/30"
          href={getWhatsAppLink("Hola, quiero cotizar un plan de rescate.")}
          target="_blank"
        >
          Cotiza aquí
        </a>
      </div>
      <div class="rounded-3xl border border-red-100 bg-white p-4 shadow-xl">
        <!-- Dummy image placeholder for replacement -->
        <img
          src="/images/plan-context-dummy.png"
          alt="Imagen dummy de plan de rescate (placeholder)"
          class="w-full rounded-2xl object-cover"
          loading="lazy"
        />
      </div>
    </div>
  </div>
</section>
