---
import { getWhatsAppLink } from '../data/config'
import { PLANES } from '../data/planes'
---

<section
  class="relative min-h-[75vh] overflow-hidden bg-white pb-10 pt-12 lg:pb-16 lg:pt-24"
  style="background-image: url('/images/3.png'); background-size: cover; background-position: center;"
>
  <!-- Decision: Hero con planes visibles para conversión inmediata y claridad de contratación. -->
  <div class="absolute inset-0 bg-white/80"></div>
  <div class="relative mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="grid gap-10 lg:grid-cols-[1.1fr_0.9fr] lg:items-center">
      <div class="order-2 text-left lg:order-1">
        <div
          class="inline-flex items-center gap-2 rounded-full border border-red-200 bg-red-50 px-3 py-1"
        >
          <span class="h-2 w-2 rounded-full bg-red-500"></span>
          <span class="text-xs font-bold uppercase tracking-[0.2em] text-red-700">
            Contratación Online Activa
          </span>
        </div>
        <h1
          class="mt-6 text-4xl font-extrabold leading-tight tracking-tight text-slate-900 sm:text-5xl lg:text-6xl"
        >
          Contrata planes de rescate en
          <span class="text-blue-700">ambulancias privadas</span>
          y servicios extrahospitalarios.
        </h1>
        <p class="mt-4 max-w-xl text-base leading-relaxed text-slate-700 sm:text-lg">
          Landing comercial exclusiva para contratación de planes de rescate, traslados programados
          y cobertura extrahospitalaria. No somos sistema de salud, no somos seguro y no atendemos
          emergencias inmediatas.
        </p>
        <div class="mt-6 flex flex-col gap-3 sm:flex-row">
          <a
            href={getWhatsAppLink('Hola, quiero cotizar un plan de rescate.')}
            class="inline-flex items-center justify-center rounded-full bg-red-600 px-7 py-3 text-sm font-extrabold text-white shadow-xl shadow-red-600/30 transition-transform hover:-translate-y-0.5"
            target="_blank"
          >
            Cotiza aquí
          </a>
          <a
            href="#planes"
            class="inline-flex items-center justify-center rounded-full border border-blue-200 bg-white px-7 py-3 text-sm font-extrabold text-blue-700 shadow-lg shadow-blue-900/5"
          >
            Contrata tu plan
          </a>
        </div>
        <p class="mt-4 text-xs font-semibold text-red-700">
          Servicio comercial de contratación. No es servicio de emergencia ni atención médica
          integral.
        </p>
      </div>

      <div class="order-1 grid gap-3 sm:grid-cols-2 lg:order-2 lg:grid-cols-1">
        {
          PLANES.map((plan) => (
            <article
              class={`rounded-2xl border ${plan.destacado ? 'border-red-200 bg-white shadow-xl shadow-red-500/10' : 'border-slate-200 bg-white'} p-4 sm:p-5`}
            >
              <div class="flex items-start justify-between gap-3">
                <div>
                  <p class="text-[11px] font-bold uppercase tracking-[0.2em] text-slate-400">
                    {plan.id === 'plan-senior' ? 'Adultos 75+' : 'Plan general'}
                  </p>
                  <h3 class="mt-2 text-xl font-extrabold text-slate-900 sm:text-2xl">
                    {plan.nombre}
                  </h3>
                  <p class="mt-2 text-xs text-slate-600 sm:text-sm">{plan.descripcion}</p>
                </div>
                <div class="rounded-2xl bg-blue-50 px-3 py-2 text-right">
                  <p class="text-xl font-extrabold text-blue-700 sm:text-2xl">
                    {plan.precio} UF
                  </p>
                  <p class="text-[10px] font-bold uppercase tracking-widest text-slate-500">Plan</p>
                </div>
              </div>
              <ul class="mt-3 space-y-2 text-xs text-slate-700 sm:text-sm">
                {plan.features.map((feat) => (
                  <li class="flex items-start gap-2">
                    <span class="mt-1 h-2 w-2 rounded-full bg-red-500" />
                    <span>{feat}</span>
                  </li>
                ))}
              </ul>
              <div class="mt-4 grid gap-2">
                <a
                  class="inline-flex w-full items-center justify-center rounded-xl bg-blue-700 py-2.5 text-xs font-extrabold text-white shadow-lg shadow-blue-700/20 sm:py-3 sm:text-sm"
                  href={getWhatsAppLink(`Hola, quiero contratar el ${plan.nombre}.`)}
                  target="_blank"
                >
                  CONTRATAR ONLINE
                </a>
                <a
                  class="inline-flex w-full items-center justify-center rounded-xl border border-slate-200 bg-white py-2 text-[11px] font-extrabold uppercase tracking-widest text-slate-700 sm:text-xs"
                  href="#planes"
                >
                  Contrata tu plan
                </a>
              </div>
            </article>
          ))
        }
        <p class="text-[11px] font-bold uppercase tracking-[0.2em] text-slate-500">
          Planes visibles desde el primer scroll · contratación directa
        </p>
      </div>
    </div>
  </div>
</section>
